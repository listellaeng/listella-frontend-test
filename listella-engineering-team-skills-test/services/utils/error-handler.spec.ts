import { errorHandler } from './error-handler';

describe('errorHandler', () => {
  it('should return successfully with an empty object', async () => {
    // const result = await  errorHandler(async () => ({}));
    // if (typeof result === 'function') {
    //   expect(result()).toEqual({});
    // }
  });
});
